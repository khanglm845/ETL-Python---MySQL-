# Bỏ qua file cấu hình môi trường (CHỨA MẬT KHẨU)
.env

# Bỏ qua thư mục môi trường ảo Python
.venv/
env/
venv/

# Bỏ qua các file rác khi chạy Python
__pycache__/
*.pyc

# Bỏ qua file log (không cần thiết đưa lên)
logs/
*.log

# (Tùy chọn) Bỏ qua dữ liệu thật nếu nó quá nặng hoặc nhạy cảm
# Em có thể giữ lại file csv mẫu nhỏ, nhưng file lớn thì nên bỏ qua
data/